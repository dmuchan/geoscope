{
  "name": "geoscope-spatial-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Web GIS spatial analysis system with memory mode and PostGIS adapter",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
